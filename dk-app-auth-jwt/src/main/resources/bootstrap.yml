spring:
  cloud:
    config:
      uri: http://localhost:8084
  application:
    name: auth
  profiles:
    active:
      - dev
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
